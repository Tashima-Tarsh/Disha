# Binary Files

The following binary files from the original repository are not included in this import:

- `frontend/app/favicon.ico` (25KB) — Next.js default favicon

To obtain these files, clone the original repository:
```bash
git clone https://github.com/Tashima-Tarsh/Tash.git
```
