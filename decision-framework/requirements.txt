# Core (always required)
# (no mandatory external packages — mock mode works out of the box)

# Optional: FAISS retriever
# pip install faiss-cpu sentence-transformers

# Optional: llama-cpp-python for local LLM
# pip install "llama-cpp-python>=0.1.71"

# Optional: OpenAI
# pip install openai

# Testing
pytest>=7.0
