FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache curl

COPY package.json ./
RUN npm install

COPY . .
RUN npm run build

RUN addgroup -S appuser && adduser -S appuser -G appuser
USER appuser

EXPOSE 3002

CMD ["npm", "start"]
