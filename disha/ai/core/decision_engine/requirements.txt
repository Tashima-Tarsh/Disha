# Core (always required)
numpy>=1.24
structlog>=21.0
pytest>=7.0

# Optional — FAISS retriever (uncomment or install separately)
# faiss-cpu>=1.7
# sentence-transformers>=2.2

# Optional — local LLM via llama-cpp-python (uncomment or install separately)
# llama-cpp-python>=0.2
