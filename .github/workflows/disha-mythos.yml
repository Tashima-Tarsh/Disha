name: Disha Mythos — Adaptive Protection & Learning

on:
  # Run daily at 3 AM UTC (after continuous training at 2 AM)
  schedule:
    - cron: "0 3 * * *"
  # Manual trigger
  workflow_dispatch:
    inputs:
      mode:
        description: "Mode: protect, learn, train, all"
        required: false
        default: "protect"
      train_rounds:
        description: "Training rounds (if train/all mode)"
        required: false
        default: "1"

permissions:
  contents: write
  security-events: read

concurrency:
  group: disha-mythos-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ── Phase 1: Threat Guardian ────────────────────────────────────────
  threat-scan:
    name: "🔒 Threat Guardian Scan"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install structlog numpy

      - name: Run Threat Guardian
        run: |
          python disha/scripts/threat_guardian.py --scan --neutralize --json > guardian_report.json 2>&1 || true
          cat guardian_report.json
        continue-on-error: true

      - name: Upload threat report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: threat-report-${{ github.run_number }}
          path: guardian_report.json
          if-no-files-found: warn
          retention-days: 30

  # ── Phase 2: Self-Healing Monitor ──────────────────────────────────
  health-check:
    name: "💊 Self-Healing Monitor"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install structlog numpy

      - name: Run Health Monitor
        run: |
          python disha/scripts/monitor.py --fix --json > health_report.json 2>&1 || true
          cat health_report.json
        continue-on-error: true

      - name: Upload health report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: health-report-${{ github.run_number }}
          path: health_report.json
          if-no-files-found: warn
          retention-days: 30

  # ── Phase 3: Mythos Pipeline ────────────────────────────────────────
  mythos-full:
    name: "🛡️ Disha Mythos Full Pipeline"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: [threat-scan, health-check]
    env:
      DISHA_MODEL_PROVIDER: mock
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install numpy structlog pytest
          pip install -r disha/ai/core/decision_engine/requirements.txt

      - name: Run Disha Mythos (protect mode on push/PR)
        run: |
          MODE="${{ github.event.inputs.mode || 'protect' }}"
          ROUNDS="${{ github.event.inputs.train_rounds || '1' }}"

          if [ "$MODE" = "all" ]; then
            python disha/scripts/disha_mythos.py --all --train-rounds "$ROUNDS" --offline --json
          elif [ "$MODE" = "train" ]; then
            python disha/scripts/disha_mythos.py --train --train-rounds "$ROUNDS" --offline --json
          elif [ "$MODE" = "learn" ]; then
            python disha/scripts/disha_mythos.py --learn --json
          else
            python disha/scripts/disha_mythos.py --protect --json
          fi
        continue-on-error: true

      - name: Upload Mythos report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mythos-report-${{ github.run_number }}
          path: mythos_report.json
          if-no-files-found: warn
          retention-days: 30

      - name: Commit any auto-fixes
        if: github.event_name == 'schedule'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add disha/scripts/ disha/services/cyber/ \
            disha/services/ai-platform/backend/ \
            disha/ai/core/decision_engine/ || true
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: disha-mythos auto-heal [run #${{ github.run_number }}]"
            git push
          fi
