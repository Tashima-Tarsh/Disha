{
  "name": "@tashima-tarsh/disha",
  "version": "6.6.0",
  "description": "DISHA v6.6 unified evidence-first intelligence product.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "test": "npm --prefix web run test",
    "type-check": "npm --prefix web run type-check",
    "audit": "npm --prefix web audit --audit-level=moderate",
    "verify": "npm --prefix web run type-check && npm --prefix web run test && npm --prefix web run build"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
